# 2026-07-21

## 冷启动优化 + 手机样式 + 单屏布局

### 1. Render 冷启动体验优化
- server.js 改用 express.static() 静态文件服务
- app.js 所有 fetch 加 15s 超时
- 添加 loading 动画和重试按钮
- 添加 favicon.svg 消除 404

### 2. 桌面端手机样式
- index.html 用 .phone 容器包裹两个模式
- styles.css 桌面端 430px 宽、圆角 48px、深色手机边框、居中
- 手机上 .phone 自动铺满全屏

### 3. 创建页面单屏适配 + 历史独立页面
- 创建页面改为 flex 全高布局，精确适配屏幕，无滚动
- 生成邀请后表单折叠（.collapsed），链接居中展示，过渡动画
- 历史记录移到独立 #historyMode 页面
- 创建页底部增加"📋 历史记录"按钮，历史页增加"← 返回"按钮
- app.js 重写 initCreatorMode，新增 showHistoryMode / loadInvitations 函数
- 表单输入框紧凑化适配小屏

### 部署
- Render 免费计划有冷启动（15 分钟无请求休眠）
- 使用 cron-job.org 白天时段 ping 可缓解
- GitHub Personal Access Token 不能进仓库（push protection 会拦截）
- 推送方式: git push https://token@github.com/zhou022222/DateInvite.git main
